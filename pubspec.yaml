name: flutter_dependcy_injection
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # dependency injection
  get_it: ^7.6.0
  injectable: ^2.1.2

  # network 
  dio: ^5.2.1+1
  json_serializable: ^6.7.0
  json_annotation: ^4.8.1

  # logger
  logger: ^1.4.0

  #equatable
  equatable: ^2.0.5

  # provider
  provider: ^6.0.5

  # router -- route management
  auto_route: ^5.0.4



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  
  # dependency injection
  injectable_generator: ^2.1.6
  
  # auto route generator
  auto_route_generator: ^5.0.3

  # building generated code
  build_runner: ^2.4.5


flutter:
  uses-material-design: true
